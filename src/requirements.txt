# Pinned exactly. This file is baked into the Lambda bundle at synth time, so an
# unpinned range means a redeploy months from now can ship a different SDK with no
# code change. The anthropic SDK's surface moves fast -- AnthropicBedrockMantle,
# output_config, and the thinking parameters have all shifted recently -- and a
# runtime break would only surface on a real video, not in CI.
anthropic[bedrock]==0.120.2
httpx==0.28.1
